name: Application Verify (Deferred)

# Automatic CI is intentionally deferred during active development.
# Re-enable push/pull_request triggers only with explicit repository-owner approval.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deferred:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Application CI is deferred. Use local verification commands during active development."
